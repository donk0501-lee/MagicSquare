# MagicSquare — `docs/` 문서 통합·작업 내역 보고서

**문서 목적:** `docs/` 디렉터리에 흩어져 있던 PRD·To-Do·리다이렉트를 **단일 파일**로 통합한 작업과, 그에 따른 저장소 전역 참조 갱신 내역을 한곳에 보존한다.  
**범위:** 통합 문서 생성, 기존 `docs/` 파일 삭제, `README.md`·`Report/5`·`pyproject.toml`·패키지 메타·프롬프트·통합 PRD 본문(§13 표) 보정.  
**비범위:** 도메인 비즈니스 로직 구현, `Report/1`~`4` 본문 개정, `Report/5` 전체 본문을 §13까지 자동 동기화하는 절차(수동 복제 시 참고만).

**작성 기준일:** 2026-04-28  

---

## 1. 배경

- `docs/`에 `5.PRD_MagicSquare_4x4_TDD.md`, `PRD_MagicSquare_4x4_TDD.md`(리다이렉트), `Implementation_Todo_MagicSquare_4x4.md`가 병존하여 **역할이 겹치고 유지 부담**이 컸다.
- README·Report·코드 주석이 서로 다른 파일명을 가리키는 경우가 생겼다.

---

## 2. 수행 작업 요약

| 구분 | 내용 |
|------|------|
| **통합 산출물** | [`docs/MagicSquare_4x4_TDD.md`](../docs/MagicSquare_4x4_TDD.md) — §1~§12: 기존 PRD, **§13**: 구현 To-Do(Epic, US, TASK-001~033 표, Dual-Track, 추적 안내), 문서 이력(버전 1.1 통합 행 포함). |
| **삭제** | `docs/5.PRD_MagicSquare_4x4_TDD.md`, `docs/PRD_MagicSquare_4x4_TDD.md`, `docs/Implementation_Todo_MagicSquare_4x4.md` |
| **품질 보정** | 통합본 생성 직후 §13.3 표에서 이스케이프·탭으로 깨진 셀을 **수동으로 바로잡음**(연결 Test·Code 열의 백틱·함수명 등). |
| **README** | 단일 진입 문서를 `docs/MagicSquare_4x4_TDD.md`로 통일; To-Do는 **§13** 참조; 오타 `PRDBR-12` → `BR-12` 수정. |
| **`Report/5. PRD_Report.md`** | “문서 관계 한 줄” 및 문서 목적·범위에 통합본·§13 To-Do 반영; 문서 이력의 `docs` 경로 갱신. |
| **기타** | `src/magic_square/__init__.py` docstring, `pyproject.toml` `description`, `Prompts/PRD_prompt.md` 내 구 경로 문구 갱신. |
| **검증** | `python -m pip install -e ".[dev]"`, `python -m pytest` 통과(스모크 테스트 유지). |

---

## 3. 통합 후 `docs/` 구조

```
docs/
└── MagicSquare_4x4_TDD.md   ← 요구·검증·To-Do 단일 원천
```

---

## 4. 문서 관계 (현재 고정)

- **정식 본문(링크·PR·에이전트 기본):** `docs/MagicSquare_4x4_TDD.md`
- **동기 사본:** [`Report/5. PRD_Report.md`](5.%20PRD_Report.md) — PRD §1~§12 본문은 Report에도 유지; **§13**은 통합본에만 있을 수 있으므로, Report 전체를 통합본과 맞출 때는 §13 복제 여부를 팀이 정한다.

---

## 5. 후속 제안 (선택)

- CI에서 `docs/MagicSquare_4x4_TDD.md` 존재 및 §13 표 헤더 존재를 가벼운 스크립트로 검사한다.
- `Report/5`를 통합본과 **완전 동일**(§13 포함)으로 맞추려면, 배포·리뷰 절차에 “Report/5 ← docs 통합본 복사” 한 단계를 둔다.

---

## 6. 참고 경로

| 항목 | 경로 |
|------|------|
| 통합 PRD·To-Do | `docs/MagicSquare_4x4_TDD.md` |
| PRD 동기 사본·관계 문구 | `Report/5. PRD_Report.md` |
| 루트 안내 | `README.md` |
